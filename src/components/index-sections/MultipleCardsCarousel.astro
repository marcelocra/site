---
const { title, href, rows = 1 } = Astro.props;
---

<div class="flex justify-between items-center">
  <h2 class="text-4xl font-bold m-6">{title}</h2>
  <a href={href}>see all</a>
</div>

<div
  class={`grid grid-flow-col grid-rows-${rows} gap-3 justify-between w-full overflow-x-scroll [&::-webkit-scrollbar]:hidden p-3`}
>
  <slot />
</div>
