---
import { defaultLocale } from "../consts";

const { date } = Astro.props;
---

<time class={Astro.props.class} datetime={date.toISOString()}>
  {
    date.toLocaleDateString(defaultLocale(Astro), {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
